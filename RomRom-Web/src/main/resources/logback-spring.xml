<?xml version="1.0" encoding="UTF-8"?>
<configuration>

  <!-- Spring Boot 기본 logback 설정 포함 (console, file 등) -->
  <include resource="org/springframework/boot/logging/logback/defaults.xml"/>
  <include resource="org/springframework/boot/logging/logback/console-appender.xml"/>

  <!-- application.yml의 logging.file.name 프로퍼티 사용 -->
  <springProperty scope="context" name="LOG_FILE" source="logging.file.name" defaultValue="romrom.log"/>

  <!-- 파일 Appender (기존 application.yml 설정 반영) -->
  <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
    <file>${LOG_FILE}</file>
    <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
      <fileNamePattern>${LOG_FILE}.%d{yyyy-MM-dd}.%i.gz</fileNamePattern>
      <maxFileSize>100MB</maxFileSize>
      <maxHistory>30</maxHistory>
    </rollingPolicy>
    <encoder>
      <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
    </encoder>
  </appender>

  <!-- 실시간 로그 스트리밍 WebSocket Appender (#788: SSE에서 전환) -->
  <appender name="WS_LOG" class="com.romrom.common.logging.LogWebSocketAppender"/>

  <!-- 로그레벨은 application-{profile}.yml의 logging.level에서 단일 관리한다.
       여기서는 Appender 정의만 담당한다. -->

  <!-- Root 로거 -->
  <root level="INFO">
    <appender-ref ref="CONSOLE"/>
    <appender-ref ref="FILE"/>
    <appender-ref ref="WS_LOG"/>
  </root>

</configuration>
