📝 현재 문제점
---

- 현재 admin 대시보드(`/admin`)는 통계 카드 4개(전체 회원 / 등록 물품 / **진행중 거래** / 신고 접수)와 최근 가입 회원·최근 등록 물품 테이블 2개만 노출한다.
- 거래가 "진행중 거래" 하나로 뭉뚱그려져 있어, **교환완료(TRADED) / 취소(CANCELED) / 채팅중(CHATTING) / 완료요청(TRADE_COMPLETE_REQUESTED) / 대기(PENDING)** 같은 상태별 현황을 한눈에 볼 수 없다.
- 작성된 후기(TradeReview) 건수가 대시보드에 전혀 드러나지 않는다.
- 통계가 **전체 누적 순간값**만 있고, 기간(오늘/7일/30일/직접선택)별로 끊어 볼 수 없다.
- 카드를 눌러 해당 현황의 상세 목록으로 바로 이동(드릴다운)할 수 없어, 좌측 메뉴를 다시 찾아 들어가야 한다.
- 결과적으로 대시보드가 "관제판"이 아니라 "요약 숫자판" 수준에 머물러 있다.

🛠️ 해결 방안 / 제안 기능
---

PostgreSQL에 **이미 존재하는** 거래/회원/물품/후기 데이터를 기반으로 대시보드를 통계 관제판으로 개편한다. (실시간 접속/방문자 분석은 신규 수집 인프라가 필요하므로 별도 이슈로 분리 — 아래 "관련 이슈" 참고.)

**확장성 원칙 — 카드/카운트를 "필드 하드코딩"이 아니라 "데이터 주도"로 설계한다.**
거래 상태 카드를 `pendingTrades`, `chattingTrades`... 처럼 응답 필드로 박으면, `TradeStatus` enum에 상태가 추가될 때마다 응답 DTO·프론트 카드를 둘 다 고쳐야 한다. 대신 **카운트를 컬렉션(Map/List)으로 반환**하고, 프론트는 그 컬렉션을 순회해 카드를 자동 렌더한다. → 새 상태/지표가 생겨도 enum만 늘리면 카드가 따라 늘어난다.

1. **거래 상태별 통계 카드 (데이터 주도)** — `TradeStatus` 전 enum을 순회해 상태별 카운트를 `Map<TradeStatus,Long>`(또는 `List<StatCardDto>`)로 반환. 프론트는 이를 loop 렌더. 기존 "진행중 거래" 단일 카드 대체. 회원/물품 카드는 유지.
2. **신규 후기 카운트 카드** — `TradeReview` 건수를 읽기 전용 숫자 카드로 추가(클릭 동작 없음). 후기 목록/숨김 기능은 별도 후기 관리 이슈로 분리.
3. **기간 필터** — `전체 / 오늘 / 7일 / 30일 / 직접선택(날짜 범위)` 토글. 선택 기간에 따라 카드 카운트와 하단 리스트가 갱신된다. "전체"는 누적값.
4. **카드 드릴다운** — 거래 상태 카드 클릭 시 해당 상태로 필터링된 거래 목록 페이지(`/admin/trades?tradeStatus=...&startDate=...&endDate=...`)로 이동. 회원/물품 카드도 각 목록 페이지로 이동. 드릴다운 링크는 카드 데이터(상태값)에서 자동 생성 — 카드마다 따로 박지 않는다.
5. **최근 교환완료 거래 리스트** — 하단에 최근 TRADED 거래(양쪽 물품/회원/완료시각)를 표시. 행 클릭 시 거래 상세로 이동.
6. **기존 위젯 유지** — "최근 가입 회원 / 최근 등록 물품" 테이블 2개는 그대로 두고, 위 신규 섹션을 그 위/아래에 배치.
7. **시계열 통계 (범용 1개 API)** — `type` 별 분기 메서드를 여러 개 만들지 않고, `metricKey`(MEMBER/ITEM/TRADE_COMPLETED/REPORT/REVIEW...) + 기간 + 버킷단위를 받는 **범용 timeseries API 하나**로 처리. 새 지표는 `metricKey` 등록 + 카운트 쿼리 1개 추가로 끝. 차트는 ApexCharts(분석 이슈 #717과 동일 스택으로 통일).

⚙️ 작업 내용
---

**Backend**
- [ ] `/dashboard/stats` 응답을 **데이터 주도 카운트 구조**로 확장
  - 거래 상태 카운트는 `TradeStatus` enum 순회로 채운 `Map<TradeStatus,Long>`(또는 `List<StatCardDto>`)로 반환 — 상태별 개별 필드 박지 않음 (enum 추가 시 무수정 확장)
  - `AdminResponse.AdminDashboardStats` 에 `tradeStatusCounts`(컬렉션) + `newReviewCount` 추가 (admin 컨벤션: 별도 DTO 금지, `AdminResponse`에 필드 추가)
- [ ] 기간 필터 지원: `/dashboard/stats` 가 `AdminRequest.startDate / endDate` 를 받아 기간 내 카운트 반환
  - 상태별 기간 카운트는 `group by tradeStatus` 단일 집계 쿼리로 한 번에 (상태마다 쿼리 분기 금지)
  - 후기 기간 카운트 쿼리 (`TradeReviewRepository.countByCreatedDateBetween`)
- [ ] 최근 교환완료 거래 조회: `POST /api/admin/dashboard/recent-trades` — TRADED 상태 최신 N건 (양쪽 물품/회원 fetch join)
- [ ] **범용 시계열 통계 API**: `POST /api/admin/dashboard/timeseries` — `metricKey`(MEMBER/ITEM/TRADE_COMPLETED/REPORT/REVIEW...) + 기간 + 버킷단위(일/시간)
  - `metricKey` → 카운트 쿼리 매핑을 **registry(Map/strategy)** 로 구성 — 새 지표는 항목 1개 등록으로 추가, switch 분기 증식 금지
- [ ] `AdminDashboardControllerDocs`(또는 해당 Docs 인터페이스) 의 `@Operation` description + `@ApiChangeLog` 최상단 갱신

**Frontend (Thymeleaf — `templates/admin/dashboard.html`)**
- [ ] 기간 필터 바(전체/오늘/7일/30일/직접선택) 추가, 선택 시 `adminFetch.post('/dashboard/stats', {startDate, endDate})` 재요청
- [ ] 거래 상태 카드를 `tradeStatusCounts` 컬렉션 **순회로 자동 렌더**(하드코딩 카드 나열 금지) + 회원/물품 카드. 각 카드 `onclick` 드릴다운 링크는 카드 데이터(상태값)에서 생성
- [ ] 신규 후기 카드(읽기 전용) 렌더
- [ ] "최근 교환완료 거래" 리스트 섹션 추가
- [ ] 기존 "최근 가입 회원 / 최근 등록 물품" 위젯 유지
- [ ] (시계열) ApexCharts 라인 차트 — 일별 추이

**검증**
- [ ] 거래 상태별 카드 합 = 전체 거래 수 정합성 검증
- [ ] 카드 드릴다운 결과 목록과 카운트 일치 검증
- [ ] 기간 필터 경계값(당일 00:00 / 23:59) 검증
- [ ] 통합 테스트

관련 이슈
---
- 부모 Epic: #707
- 후기 카드 클릭 시 연결될 후기 관리(soft delete): **#771** — 본 이슈에서는 후기 카운트만 노출
- 실시간 접속/방문자 분석(현재 온라인/방문자/시간별 동접): **#772** — 본 이슈 범위 제외

🙋‍♂️ 담당자
---
- 백엔드: SUH SAECHAN
- 프론트엔드: 미정
- 디자인: 미정
