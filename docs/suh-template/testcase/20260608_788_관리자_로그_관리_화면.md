## 테스트케이스: 관리자 로그 관리 화면

| 구분 | 내용 |
|------|------|
| 이슈 번호 | #788 |
| 대상 | 관리자 > 로그 관리 (`/admin/logs`) |
| 담당자 | @Cassiiopeia |
| 작성일 | 2026-06-08 |

---

### TC-01: 로그 관리 메뉴 진입

| 항목 | 내용 |
|------|------|
| 전제조건 | 관리자 로그인 상태 |
| 절차 | 관리자 사이드바 → `로그 관리` 클릭 |
| 기대 | `/admin/logs` 진입, 상단 탭 4개(조회·검색 / 에러 대시보드 / 실시간 / 파일·다운로드) 표시, 진입 시 조회·검색 탭 기본 활성 |
| 결과 | |

### TC-02: 로그 조회 + 레벨 필터

| 항목 | 내용 |
|------|------|
| 전제조건 | 서버에 ERROR/INFO 로그가 적재된 상태 |
| 절차 | 조회·검색 탭 → 레벨 `ERROR` 선택 → 줄수 `200` → `검색` |
| 기대 | ERROR 라인만 출력, ERROR 빨강·WARN 노랑 색상 표시, 시간순(오래된→최신) |
| 결과 | |

### TC-03: 키워드 검색

| 항목 | 내용 |
|------|------|
| 절차 | 조회·검색 탭 → 키워드란에 특정 단어(예: `Exception`) 입력 → `검색` |
| 기대 | 해당 키워드 포함 라인만 표시 |
| 결과 | |

### TC-04: 에러 대시보드 집계 + 점프

| 항목 | 내용 |
|------|------|
| 전제조건 | 최근 1시간 내 ERROR 로그 존재 |
| 절차 | 에러 대시보드 탭 → 기간 `최근 1시간` → `집계` → 표의 한 행 클릭 |
| 기대 | 예외 클래스별 발생횟수/마지막발생/대표메시지 표시(발생횟수 내림차순), 행 클릭 시 조회 탭으로 이동하며 해당 예외명으로 자동 검색 |
| 결과 | |

### TC-05: 실시간 라이브 tail

| 항목 | 내용 |
|------|------|
| 절차 | 실시간 탭 클릭 → 서버에서 로그 발생 유도(API 호출 등) |
| 기대 | 상태 배지 `연결됨`, 새 로그가 흐르듯 출력, `자동 스크롤` 체크 시 하단 자동 이동 |
| 결과 | |

### TC-06: 실시간 탭 이탈 시 SSE 해제 (부하 최소화)

| 항목 | 내용 |
|------|------|
| 전제조건 | 브라우저 개발자도구 Network 탭 열기 |
| 절차 | 실시간 탭 진입(stream 연결 확인) → 다른 탭으로 이동 → 브라우저 탭을 백그라운드로 전환 |
| 기대 | 실시간 탭 이탈/백그라운드 시 `logs/stream` 연결 종료, 실시간 탭 복귀 시 재연결. 로그 폭주 시에도 화면 라인 500줄 초과분은 위에서 제거(브라우저 멈춤 없음) |
| 결과 | |

### TC-07: 파일 목록 + 용량/디스크 상태

| 항목 | 내용 |
|------|------|
| 절차 | 파일·다운로드 탭 클릭 |
| 기대 | 현재 `romrom.log` + 과거 `.gz` 목록(파일명/크기/수정시각, 최신순), 상단에 로그 총 용량·파일 수·디스크 여유공간 표시 |
| 결과 | |

### TC-08: 시간범위 다운로드

| 항목 | 내용 |
|------|------|
| 절차 | 파일·다운로드 탭 → `최근 1시간` 버튼 클릭 |
| 기대 | `romrom-log_1h_{시각}.log` 파일 다운로드, 내용은 최근 1시간 라인. 범위 내 로그 없으면 "범위 내 로그 없음" 헤더 포함 |
| 결과 | |

### TC-09: 파일 통째 다운로드 (.log / .gz)

| 항목 | 내용 |
|------|------|
| 절차 | 파일 목록의 `romrom.log` 및 임의 `.gz` 행에서 `다운` 클릭 |
| 기대 | 해당 파일 그대로 다운로드 |
| 결과 | |

### TC-10: .gz 압축 해제 조회

| 항목 | 내용 |
|------|------|
| 전제조건 | 과거 `.gz` 롤링 파일 존재 |
| 절차 | 파일 목록의 `.gz` 행 → `조회` 클릭 |
| 기대 | 압축 해제된 로그 내용 표시(레벨/키워드 필터 적용 가능), 브라우저로 gz 원본을 통째 전송하지 않음 |
| 결과 | |

### TC-11: 비인증 접근 차단

| 항목 | 내용 |
|------|------|
| 전제조건 | 로그아웃 상태 또는 토큰 없음 |
| 절차 | `POST /api/admin/logs/query` 직접 호출 |
| 기대 | `401 Unauthorized`, 데이터 미반환 |
| 결과 | |

### TC-12: Path Traversal 차단

| 항목 | 내용 |
|------|------|
| 전제조건 | 관리자 인증 상태 |
| 절차 | `GET /api/admin/logs/download-file?fileName=../secret.txt` 직접 호출 |
| 기대 | `400 Bad Request`(INVALID_REQUEST), 로그 디렉터리 밖 파일 접근 차단 |
| 결과 | |

### TC-13: Swagger 문서 일치 (Backend 공통)

| 항목 | 내용 |
|------|------|
| 절차 | Swagger UI(`/docs`)에서 `/api/admin/logs/*` 엔드포인트 확인 |
| 기대 | query/errors/files/gz-query/download/download-file/stream 7종 노출, `@Operation` 설명·`@ApiChangeLog`(#788) 반영 |
| 결과 | |
